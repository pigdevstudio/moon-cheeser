extends Node

onready var animator = $AnimationPlayer

func play_track(track = ""):
#	if has_node(track):
#		animator.play(track)
#		track = get_node(track)
#		track.play($Bass.get_playback_position())
	pass

func stop_track(track = ""):
#	if has_node(track):
#		animator.play_backwards(track)
#		track = get_node(track)
#		yield(animator, "animation_finished")
#		track.stop()
	pass
